# 工伴·桌面精灵

## 让 AI Agent 进入真实工作现场

工伴（小智）是一只面向工程师的 AI 桌面宠物：它能感知人在不在岗，读懂 Codex、Claude Code 等 Agent 的工作状态，汇总飞书和线上告警，并通过桌面界面与 ESP32-S3 双轴机器人，在合适的时机提醒、陪伴和反馈。

我们想解决的不是“再做一个通知工具”，而是让工程师在多 Agent、多消息、强打断的工作环境中，始终知道：**现在发生了什么、是否需要我介入、下一步该做什么**。

## 工程师与小智相处的一天

| 场景 | 小智做什么 | 用户得到的价值 |
| --- | --- | --- |
| 早晨到岗 | 识别主人、亮屏、转向并欢迎；陌生人只得到通用回应 | 让工作从被看见开始，而不是从一堆未读消息开始 |
| 晨间简报 | 聚合飞书消息、日程和任务，按截止时间、协作影响和线上风险筛出最重要的三件事 | 把“信息很多”变成“先做什么” |
| 开始工作 | 接收 Codex / Claude Code / WorkBuddy 的运行、完成、失败和待介入状态；配合番茄钟工作 | 不用频繁切屏，也能知道 Agent 是否在等人 |
| 权限请求 | 先说明“谁要做什么、风险在哪里”，再由工程师确认；机器人不替人批准高风险操作 | 保留自动化效率，也保留人的最终控制权 |
| 开会离席 | 记录离席状态，保留 Agent 结果、告警和留言；返岗后按严重度和优先级一次性汇总 | 离开工位不会错过真正重要的事 |
| 专注陪伴 | 专注模式下减少普通打扰，只在必要时提醒休息或处理关键事项 | 提醒有分寸，不把陪伴变成监控 |
| 线上故障 | 接收告警、合并重复事件，关联日志和最近变更；在人工确认后调用只读诊断 Agent，并持续观察恢复 | 从“告警轰炸”升级为“事实、建议、恢复时间线” |
| 下班与请假 | 汇总当天完成事项，记录未来离岗状态；设备继续守住工位，重视留言和紧急信息 | 工作可以结束，重要上下文不会丢 |

## 技术实现

```text
Codex / Claude Code / WorkBuddy
          │ Hook 事件
          ▼
Electron 桌面端 ── HTTP / 摄像头 WebSocket ──► Server
          │                                      │
          │                                      ├─ 飞书晨报与任务聚合
          │                                      ├─ 在岗 / 主人识别与离席台账
          │                                      ├─ 告警中继与只读诊断 Agent
          │                                      └─ 规则编排、记忆与权限边界
          │                                      │
          └──────────────────────────────────────┘
                         WebSocket + MCP
                                  ▼
                        ESP32-S3 双轴机器人
```

- **桌面端**：Electron + React + TypeScript。通过 Hook 原子采集 Agent 事件，在本机判断运行中、已完成、失败和需要用户介入；摄像头以低帧率内存流发送，不落盘。
- **Server**：系统的主要大脑，统一接入工作事件、飞书数据、摄像头在岗状态和监控告警，负责确定性排序、提醒节流、离席汇总、告警状态机和对话能力。
- **机器人端**：ESP32-S3 通过 WebSocket 常连，只执行经过测试的表情、点头、转向、播报等动作，不承担 LLM 判断或高风险决策。
- **安全边界**：权限申请和生产操作始终由人确认；告警诊断默认只读；摄像头原始帧只在桌面端与配置的 Server 间以内存传输，不保存完整代码、提示词或对话。

## 产品价值

1. **把 Agent 从“后台进程”变成“可感知的工作伙伴”**：工程师不必守着每个终端，机器人用表情、动作和短语表达状态。
2. **把多源信息压缩成行动顺序**：飞书、Agent、摄像头和监控告警进入同一条上下文链路，减少切屏和遗漏。
3. **把提醒做成有分寸的协作**：普通事件延后或汇总，只有权限请求和严重告警打断专注；设备离线也不积压过期动作。
4. **把自动化放在人之下**：小智可以解释、建议和诊断，但不擅自批准权限、不自动修改生产环境。
5. **同时提供效率价值与情绪价值**：它既能守住线上故障和工作上下文，也能在到岗、完成和返岗时给出真实、可见的陪伴反馈。

## 当前演示重点

当前仓库已经接通的主链路包括：Agent Hook 监控、飞书任务与晨报、桌面摄像头在岗与主人识别、离席事件汇总、线上告警中继、人工确认后的只读诊断，以及 Server 到 ESP32-S3 机器人的动作与语音下发。

“手机分心识别、语音留言、未来请假等”属于完整产品叙事中的扩展场景，适合在演示视频中作为下一步能力呈现；预选说明以已经接通的核心链路为可信基线。

**一句话总结：工伴不是替工程师工作的另一个 Agent，而是帮助工程师看见 Agent、管理打扰、守住风险，并在忙碌的一天里始终有人陪伴的工作伙伴。**
